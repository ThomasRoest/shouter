<%= div_for shout do %> 
<%= gravatar shout.user %>
	<%= link_to shout.user.username, shout.user %><%# div for view helper, convenient for styling as well %>
	<div>
	<%= render shout.content %>
	</div>
	<%= link_to time_ago_in_words(shout.created_at), shout %>
<% end %>